interface IPathname {
  home: string;
  about: string;
  product: string;
  stores: string;
  contacts: string;
}

const Pathname = {
  home: "/",
  about: "/about",
  product: "/products",
  stores: "/stores",
  contacts: "/contacts",
};

export default Pathname;
